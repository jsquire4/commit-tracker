spring:
  datasource:
    # Overridden by Testcontainers dynamically
    url: jdbc:tc:postgresql:16-alpine:///compass_test
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false

compass:
  seed:
    enabled: false

logging:
  level:
    com.compass.platform: WARN
    org.hibernate.SQL: WARN
