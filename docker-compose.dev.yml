services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: st6
      POSTGRES_USER: st6
      POSTGRES_PASSWORD: st6local
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
